# Wails build output
bin/
Loadout.exe
# `go build ./...` writes the module-named binary next to the sources
# (the root .gitignore pattern `loadouttool/*.exe` is a directory pattern and
# does not match this file).
loadouttool.exe

# Frontend
frontend/node_modules/
frontend/dist/
frontend/bindings/


# Dev-run data copies (next to Loadout.exe). 必须锚定到本目录：不带斜杠的 `gem.json`
# 会连 assets\gem.json 一起忽略，而 assets\ 下的数据全部入库。
/gem.json
/gem.chara.json
Loadout.exe~


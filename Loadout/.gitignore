# Wails build output
bin/
Loadout.exe
# `go build ./...` writes the module-named binary next to the sources.
# 根 .gitignore 曾经也有一条 `loadouttool/*.exe`（目录形式，永远匹配不到这个文件），
# 已删除——这条才是生效的那一条。
loadouttool.exe

# Frontend
frontend/node_modules/
frontend/dist/
frontend/bindings/

# 编辑器残留。
Loadout.exe~


# Wails build output
bin/
Loadout.exe
# `go build ./...` writes the module-named binary next to the sources
# (the root .gitignore pattern `loadouttool/*.exe` is a directory pattern and
# does not match this file).
loadouttool.exe

# Frontend
frontend/node_modules/
frontend/dist/
frontend/bindings/


# Local runtime data copies (next to Loadout.exe for dev runs)
gem.json
character-exclusives.json
Loadout.exe~

